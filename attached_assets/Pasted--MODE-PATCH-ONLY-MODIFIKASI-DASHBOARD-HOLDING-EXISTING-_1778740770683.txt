[MODE] PATCH ONLY — MODIFIKASI DASHBOARD HOLDING EXISTING
[KONTEKS] Dashboard Holding sudah ada dan bisa akses 4 perusahaan via filter. Database: Supabase.
[TUJUAN] Ubah perilaku default dashboard agar MENAMPILKAN DATA GABUNGAN (MERGED) dari ke-4 perusahaan sekaligus. Filter perusahaan tetap ada sebagai opsi tambahan.

🎯 PERUBAHAN UTAMA YANG DIMINTA:

1. DEFAULT VIEW = "ALL COMPANIES" (MERGED)
   - Saat user (divatranssoetta@gmail.com) buka dashboard, JANGAN default ke filter perusahaan tertentu.
   - Langsung tampilkan tabel COA & Transaksi yang sudah digabung dari ke-4 perusahaan.
   - Query Supabase: HILANGKAN filter `.eq('company_id', ...)` saat user adalah super admin.
   - Gunakan `.in('company_id', [id1, id2, id3, id4])` atau biarkan kosong untuk ambil semua.

2. KOLOM "PERUSAHAAN" WAJIB ADA
   - Karena data digabung, tambahkan kolom pertama di tabel: "Asal Perusahaan".
   - Tampilkan nama perusahaan (bukan ID) agar mudah dibaca.

3. FILTER TETAP BERFUNGSI (SEBAGAI PEMBATAS)
   - Dropdown filter perusahaan tetap ada di atas tabel.
   - Default value dropdown: "🔗 Semua Perusahaan (Gabungan)".
   - Jika user pilih salah satu perusahaan → tabel update menampilkan data perusahaan itu saja.

4. VISUAL INDICATOR
   - Tambah badge/chip di atas tabel: "👁️ Menampilkan: Data Gabungan 4 Perusahaan".

5. FITUR EXPORT
   - Tombol "Export to Excel/CSV" harus mengekspor data yang sedang tampil (merged atau filtered).
   - Pastikan kolom "Perusahaan" ikut terekspor.

🗄️ BACKEND LOGIC (Supabase/Node.js):
```javascript
app.get('/api/holding/transactions', async (req, res) => {
  const { userEmail, filterCompanyId } = req.query;
  let query = supabase.from('transactions').select('*, companies(name)');
  
  if (userEmail === 'divatranssoetta@gmail.com') {
    // Super Admin: Default ambil SEMUA, KECUALI jika ada filter spesifik
    if (filterCompanyId && filterCompanyId !== 'all') {
      query = query.eq('company_id', filterCompanyId);
    }
    // Jika 'all' atau undefined → tidak ada where clause company_id
  } else {
    // User biasa: Wajib filter sesuai company_id mereka
    query = query.eq('company_id', req.user.company_id);
  }
  const { data, error } = await query;
  res.json({ success: !error, data });
});